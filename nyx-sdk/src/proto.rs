#![forbid(unsafe_code)]

//! Placeholder for prost-generated types or flattened JSON structures.
//! gRPC is disabled by default in this workspace to avoid ring/openssl.

#[allow(dead_code)]
pub mod types {
	#[derive(Debug, Clone)]
	pub struct Placeholder;
}

